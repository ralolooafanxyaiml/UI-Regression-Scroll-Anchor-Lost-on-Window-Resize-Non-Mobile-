# UI-Regression-Scroll-Anchor-Lost-on-Window-Resize-Non-Mobile-
Identified a UI regression bug in the Gemini web interface where resizing the browser window causes the chat scroll position to incorrectly reset to the top. This issue indicates a loss of scroll anchoring during the DOM re-render, directly impacting User Experience (UX) and session continuity.
